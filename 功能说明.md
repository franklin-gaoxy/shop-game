# 交易小游戏

<br />

编写一个“交易小游戏”。

## 后端需求描述：

1. 后端使用go语言和cobra框架，使用gin框架作为接口，使用mysql作为数据存储（后续可能需要支持其他类型数据库，所以这里使用接口），mysql信息从 config.yaml文件获取。
2. 后端程序启动后首先链接数据库、检查环境是否满足运行标准。
3. 后端代码需要拆分开，gin相关实现代码单独一个包、数据库操做相关单独一个包。
4. 这是一个“交易游戏”所以必须具备 买入 和 卖出 两个功能，通过这个过程中的插件来谋利，用户会有一个初始金钱default\_money字段，同时，还有两个仓库用来存储对应的物品，分别是普通仓库，字段default\_warehouse，以及一个冷库default\_cold\_storage，数字分别代表其大小。
5. platform字段对应的是平台管理员的用户名和密码。
6. 平台需要支持多用户，不同用户的用户名、密码等都不相同，所以需要一个user表来存储用户的信息，包含他们的剩余金钱、仓库大小、对应天数等元数据。
7. 接下来就是商品信息了，需要一张商品信息的元数据表。你先随机生成一些商品，如西红柿、汉堡、钢铁、芯片等，然后他们需要一个最低价和最高价，商品的价格在这个范围里取随机值，同时需要一个占用仓库大小的字段，不同种类的商品占用仓库的大小也是不同的，同时需要一个类型字段，用来记录这个商品是应该用 普通仓库 存储还是 冷藏仓库 存储，或者支持两者存储。需要注意的是，还需要设置一个 过期时间，不同的仓库过期时间也不同，比如商品 西红柿 存储在普通仓库的过期时间7天，但是存储在冷藏仓库可以是30天，所以需要两个字段来记录不同的时间，同时还有一些商品如 钢铁 只能存储在普通仓库，而且期限可以是永久。最后，还需要设置商品的类型和一个 暴击最小值 和 暴击最大值，这两个用来控制事件暴击，如可能因为某地发生灾情，所以食物类的回普遍涨价，涨价就是“暴击”行为，它的价格会大幅增长，而增长的价格则在 暴击最小值 和 暴击最大值 之间取随机值，暴击事件不仅针对类型，也可能针对某一个单一的商品。
8. 前面提到了“暴击事件”所以还需要一张存储暴击信息的元数据表，需要有对应的事件说明信息列、暴击针对商品种类或者单一商品、暴击发生的概率等字段。
9. 接下来还有个功能是“明天”，当触发此接口时，将用户的天数+1，同时从商品信息元数据表获取数据，来根据金额范围随机为每个商品生成一个随机价格，完成后开始从暴击元数据表获取信息，根据不同的概率来随机是否需要发生暴击，如果发生了，那么则重新更新对应商品或者对应种类的价格。这里需要注意，暴击率应该是一个数字如10，则表示发生此事件的概率为10%，你可以生成一个0-100内的随机数，来判断生成的这个数字是否小于10，如果小于则触发，如果大于则不触发，同时还需要一个每日商品的表，来记录这一天为所有商品生成的随机价格范围，确保一致性。
10. 用户默认的仓库空间可能不够用，所以还需要一个特殊商城，特殊商城内可以购买仓库空间或者冷藏仓库空间，需要注意，除了已有的默认的仓库大小，后续购买的仓库大小都是有时间限制的，最短时间30天（1个月），最多可以购买12个月，所以当每天更新的时候，也需要更新仓库剩余的时间，检查是否到期，仓库可能会购买多笔，如第一天购买了30天的1000大小的仓库，第十天又购买了30天的3000大小的仓库，两个不要产生冲突。
11. 用户还需要一张表来存储自己的购买记录和仓库内已存储的数据的信息。购买记录需要体现每一笔交易是支出或者是收入、价格、买卖商品信息等，而仓库表则需要记录下来已经买入的所有的商品的信息和价格，需要注意的是可能同一件商品如 西红柿 第一天价格是10元买入了100，第二天的价格是5元又买入200，仓库表记录的价格应该是西红柿的总价值，并且计算对应的单价是多少，方便用户选择高点卖出。
12. 因为是多用户平台，所以需要支持用户注册功能，用户注册时填写用户名和密码，同时还需要填写一个密钥。密钥是10位长度的随机字符串，由系统生成，所以还需要一张表来记录密钥信息，一个密钥可以支持1个或者多个用户注册时使用，如果注册没有密钥则不允许，如果密钥达到了使用次数后也禁止掉，同时在用户表记录下来每个用户注册时使用的密钥，默认的admin(管理员)用户可以创建密钥，所以在创建密钥时需要确认用户是否是admin，admin用户默认的密钥初始化时随机生成一个即可，同时记录到密钥表。同时当遇到请求的时候需要检查是否合法、是否已经登陆。
13. 后端还需要一个子命令init来初始化数据库，读取config.yaml文件来连接到数据库，导入相关数据，管理员用户名密码信息需要从config.yaml文件读取信息来初始化，而商品对应的信息则单独准备一个yaml文件，读取里面的所有内容来导入到数据库，暴击信息也从这个单独的yaml中读取写入数据库，以及默认的仓库大小的数据，也都从这个yaml中提取，目前还没有，所以需要你生成一个用于测试使用的yaml文件。如果初始化时，发现库表数据已经存在了，则不要执行任何操做，如果增加了参数--force，则删除已有的信息重新初始化，所以还需要一个单独的sql文件。

<br />

## 前端需求

前端需要支持两种不同的模式。

1. 命令行操做
2. web页面操做

### 命令行操做需求描述

1. 绘制Linux下的表格，提示用户输入对应的操做的序号，来执行不同的内容
2. 使用golang语言，cobra框架，用户端需要一个参数来指定连接到的服务端的地址
3. 主页面操做：1. 交易 2. 明天 3. 特殊商品 4. 退出
   1. 交易对应的详细页面：1. 列出商品 2. 购买 3. 卖出 4. 查看仓库 5. 返回上一页
   2. 如果选择了明天，那么直接更新数据，并且输出一下更新到明天，以及对应的天数。
   3. 特殊商品对应的详细页面：1. 查看现有仓库空间大小 2. 购买普通仓库 3. 购买冷藏仓库 4. 返回上一页
4. 连接到服务器后提示两个选项 1. 登录 2. 注册
   1. 如果是登录可以允许输入用户名和密码来登录
   2. 如果是注册，则需要自己填写用户名和密码，用户名检查不能重复，同时密码进行md5加密存储，同时需要输入对应的密钥。

<br />

### web页面操做需求描述

1. 首先是登录页，两个按钮 登录 和 注册，对应不同的表单，默认展示登录表单，输入用户名密码，然后还要有一个前端全局设置来指定后端服务器的地址端口。
2. 注册则需要输入用户名、密码、密钥信息，提交后端检查注册结果。
3. 随后进入主页，顶部是一个标题栏，分别展示 首页、商城、特殊商城、仓库、交易记录，右侧则是一个设置按钮。
   1. 首页则展示剩余的金钱、进行到的天数、仓库的使用率、已用空间和剩余可用空间等信息。
   2. 商城页面则展示全部的商品数据，默认每页20个，每行的信息如果过长，则在底部增加滚动框允许拖动，同时前面的商品名称列固定冻结。
   3. 特殊商城展示特殊商品的信息，如上。
   4. 仓库页面则展示全部的商品信息，默认每页20个，每行的信息如果过长，则在底部增加滚动框允许拖动，同时前面的商品名称列固定冻结，需要展示不同商品的均价，同时每行尾部增加一个标志，如果该商品的均价比今天商城里的价格更高，则是一个绿色向上的小箭头，后面增加如果卖出预计可以获利多少，同时整行使用绿色文字，如果是均价比目前价格更低，那么则是向下的红色小箭头，后面展示亏损金额，同时整行使用红色字体。
   5. 交易记录页面展示全部的交易记录，默认每页20个，每行的信息如果过长，则在底部增加滚动框允许拖动，同时前面的商品名称列固定冻结，需要展示每次交易的数量、金额、交易后的余额等信息。

